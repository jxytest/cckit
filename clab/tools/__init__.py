"""Built-in tool collections for clab.

These are optional — import only what you need::

    from clab.tools.platform import get_platform_mcp_server

    runner = Runner(
        mcp_servers={"platform": get_platform_mcp_server},
    )
"""
