"""Built-in tool collections for cckit.

These are optional — import only what you need::

    from cckit.tools.platform import get_platform_mcp_server

    runner = Runner(
        mcp_servers={"platform": get_platform_mcp_server},
    )
"""
